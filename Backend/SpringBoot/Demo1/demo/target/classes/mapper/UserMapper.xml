<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="demo.demo.Mapper.UserMapper">
    <select id="selectAll" resultType="demo.demo.Pojo.User">
        SELECT * FROM USER
    </select>

    <select id="select" resultType="demo.demo.Pojo.User">
        SELECT DISTINCT * FROM USER WHERE USERNAME = #{USERNAME}
    </select>
    
    <insert id="insert">
        INSERT INTO USER (USERNAME, PASSWORD, EMAIL) VALUES (#{USERNAME}, #{EMAIL}, #{PASSWORD})
    </insert>
 
    <delete id="delete">
        DELETE FROM USER WHERE USERNAME = #{USERNAME}
    </delete>
 
    <update id="update">
        UPDATE USER SET EMAIL = #{EMAIL} WHERE USERNAME = #{USERNAME}
    </update> 
</mapper>